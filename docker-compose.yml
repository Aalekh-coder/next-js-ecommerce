version: '3.8'

services:
  postgres:
    image: postgres:13
    restart: always
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: A@lekh8254
      POSTGRES_DB: EcommerceNextjs
    ports:
      - '5436:5432'
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data: